spring:
  application:
    name: live-test-service
    
server:
  port: 8088

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka

logging:
  level:
    org:
      type:
        description:
          sql: trace
      hibernate:
        SQL: debug
        type:
          description:
            sql: trace